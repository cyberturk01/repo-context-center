# Risk Register

Track areas where changes need extra care.

Format:

| Area | Risk | Check |
| --- | --- | --- |
| `path/or/system` | What can break | Test or review needed |

Include:
- Security-sensitive code.
- Data migrations.
- Public APIs.
- Cross-cutting utilities.
- Flaky or expensive tests.

Keep risks current and specific.

<!-- repo-context-center:generated:start -->
## Generated Repo Map

| Area | Why risky | First checks |
| --- | --- | --- |
| CLI: `src/cli/commands/archive.ts`, `src/cli/commands/estimate.ts`, `src/cli/commands/init.ts` | CLI behavior changes can break scripts, help text, JSON output, or exit codes. | npm run build; node --test tests/archive.test.js tests/cli.test.js; npm run lint |
| Configuration: `package-lock.json`, `package.json`, `src/cli/commands/validate.ts` | Config mistakes can misroute agent work or break validation. | npm run build; node --test tests/validate.test.js; npm run lint |
| Repository scanning: `src/cli/commands/scan.ts`, `src/core/scanner.ts`, `src/templates/generic/docs/ai-context/SYMBOL_MAP.md` | File classification changes can cause future agents to read too much or miss important files. | npm run build; node --test tests/scan.test.js; npm run lint |
| Risk rules: `src/cli/commands/validate.ts`, `src/core/validator.ts`, `src/templates/generic/docs/ai-context/HOTSPOTS.md` | Risk guidance affects what agents inspect before changes. | npm run build; node --test tests/validate.test.js; npm run lint |
| Report generation: `src/cli/commands/archive.ts`, `src/cli/commands/estimate.ts`, `src/cli/commands/map.ts` | Report rendering changes can break generated markdown, JSON consumers, or marker preservation. | npm run build; node --test tests/archive.test.js tests/estimate.test.js; npm run lint |
| Context docs: `AGENTS.md`, `docs/ai-context/CHANGE_LOG.md`, `docs/ai-context/COMMUNICATION_MODE.md` | Context doc changes affect future agent routing and token use. | npm run build; npm test; npm run lint |
| Fixture/snapshot drift: `tests/archive.test.js`, `tests/cli.test.js`, `tests/estimate.test.js` | Fixtures and expected output can drift from generated map behavior. | npm run build; node --test tests/archive.test.js tests/cli.test.js; npm run lint |

_Generated by repo-context-center. Edit outside this section._
<!-- repo-context-center:generated:end -->
